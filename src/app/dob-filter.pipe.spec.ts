import { DobFilterPipe } from './dob-filter.pipe';

describe('DobFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DobFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
